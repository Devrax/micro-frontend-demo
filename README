# Micro frontend

If you want to see how the code is working and how it interacts each other, just clone, install dependencies running `npm run prepare`, later for up and running `npm run starts`.

That's all :)